process make_cloudgene {

    input:
    val(okay)


"""

${params.cloudgene_exe} install ${params.install_dir}/cas_dataset_apps/${params.install_cas_variant}/cloudgene.yaml
echo "::message:: Cloudgene reboot is necessary to access the novel dataset" 
"""
}
